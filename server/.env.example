# Copy this file to ".env" (same folder) and fill in your real key there.
# ".env" is gitignored — it never gets committed or sent anywhere by this app.
# Get a free key at https://aistudio.google.com/apikey

GEMINI_API_KEY=paste-your-real-key-here

# Optional: override the model without touching app.py.
# GEMINI_MODEL=gemini-2.5-flash
